_config:
  - $desc: Global setting
    defaultPrefix: Add /webhook prefix
    printData: Print received data
    printResult: Print formatting results
  - $desc: Listener settings
    listeners: 
      $desc: List of listeners
      url: Listening address
      method: Listening methods
      headers: Request header verification
      pushChannelIds: 
        $desc: Push channel list
        $inner: Channel ID
      pushPrivateIds: 
        $desc: Pushed private list
        $inner: User ID
      msg: Push message
